const readline = require('readline-sync')
// tamplate swing = ${variavel}
// operador ternário ? :

let numero = 2

numero % 2 == 0 ? console.log(`O numero ${numero} é par`) : console.log(`O numero ${numero} é ímpar`)